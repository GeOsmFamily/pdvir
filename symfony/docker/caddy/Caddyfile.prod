import Caddyfile.base

(spa) {
	try_files {path} /
	file_server
	encode gzip
}

{$CADDY_PROTOCOL}://{$DOMAIN} {
	root * /var/www/vue
	import spa
}

{$CADDY_PROTOCOL}://docs.{$DOMAIN} {
	root * /var/www/docs
	import spa
}