<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Plateforme Urbaine du Cameroun - Export de carte</title>
    <style>
        @page { 
            size: A4 landscape; 
            margin: 0; 
        }

        body {
            margin: 0;
            padding: 0;
            width: 29.2cm;
            height: 20.9cm;
            font-family: Arial, sans-serif;
        }

        .MapExport {
            display: flex;
            flex-direction: column;
            align-items: center;
            width: 100%;
            height: 100%;
            box-sizing: border-box;
        }

        .MapExport__header {
            width: 100%;
            height: 7%;
            font-weight: bold;
            font-size: 1.5rem;
            display: flex;
            align-items: center;
            justify-content: center;
            border-bottom: 1px solid #000;
        }

        .MapExport__content {
            display: flex;
            width: 100%;
            height: 93%;
            box-sizing: content-box;
        }

        .MapExport__mapCtn {
            width: 73%;
            height: 100%;
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat; 
        }

        /* .MapExport__mapCtn img {
            max-width: 100%;
            max-height: 100%;
            display: block;
        } */

        .MapExport__sidebar {
            width: 25%;
            height: 100%;
            padding: 10px;
            box-sizing: border-box;
            font-size: 0.9rem;
            overflow: hidden;
            page-break-inside: avoid;
        }

        .MapExport__sidebar ul {
            list-style: none;
            padding: 0;
            margin: 0;
        }

        .MapExport__sidebar li {
            margin-bottom: 5px;
        }
    </style>
</head>
<body>
    <div class="MapExport">
        <div class="MapExport__header">{{ title }}</div>
        <div class="MapExport__content">
            <div class="MapExport__mapCtn" style="background-image: url('{{ mapUrl }}')">
            </div>
            <div class="MapExport__sidebar">
                <ul>
                    {% for item in legendList %}
                        <li>{{ item.name }}</li>
                    {% endfor %}
                </ul>
                <p>{{ description }}</p>
            </div>
        </div>
    </div>
</body>
</html>
